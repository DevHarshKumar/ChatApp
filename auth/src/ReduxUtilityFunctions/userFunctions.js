import { useDispatch } from "react-redux"
import { login } from "../Redux/userSlice";
const dispatch=useDispatch();
export default fetchUserInfo=async(dispatch)=>{
    try {
        
    } catch (error) {
        
    }
}